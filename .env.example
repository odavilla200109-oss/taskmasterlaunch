# ─────────────────────────────────────────────
#  TaskMaster — Variáveis de Ambiente
#  Copie este arquivo para .env e preencha.
# ─────────────────────────────────────────────

# Servidor
PORT=3001
NODE_ENV=development

# JWT — gere um segredo forte:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=TROQUE_POR_UM_SEGREDO_FORTE_DE_64_CHARS
JWT_EXPIRES_IN=7d

# Google OAuth
# Crie em: https://console.cloud.google.com/apis/credentials
# Tipo: "ID do cliente OAuth 2.0" → Aplicativo da Web
# URIs de redirecionamento autorizados: http://localhost:5173 (dev)
GOOGLE_CLIENT_ID=SEU_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# CORS — URLs do frontend permitidas (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Banco de dados SQLite (caminho do arquivo)
# Para produção, migre para Postgres e ajuste db.js
DB_PATH=./data/taskmaster.db
